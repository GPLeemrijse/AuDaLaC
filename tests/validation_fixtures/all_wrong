struct Node (reachable : Bool, reachable: Bool) {
    init {
        Nat n1 := 1;
        Nat n1 := 2;
        Nat n2 := 3;
    }
    step_node {
        Nat n2 := 0;
        Int n3 := 10;
        if true then {
            Int n3 := 5;
        }
    }

    init {

    }
}

struct Node(test : Nat) {
    
}

init < Fix(step_node)
